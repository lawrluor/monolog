import { sizes, colors } from './';

const baseContainer = {
  backgroundColor: 'transparent',
  flex: 1,
  // width: sizes.FULL
}

export const containers = {
  DEFAULT: {
    ...baseContainer,
  }
}