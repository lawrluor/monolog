import { colors } from './colors';
import { dimensions } from './dimensions';

export const icons = {
  HUGE: {
    color: colors.HIGHLIGHT,
    fontSize: dimensions.width / 3.94 // 96
  },
  LARGE: {
    color: colors.HIGHLIGHT,
    fontSize: dimensions.width / 5.91 // 64
  },
  MEDIUM: {
    color: colors.HIGHLIGHT,
    fontSize: dimensions.width / 10.5 // 36
  },
  SMALL: {
    color: colors.HIGHLIGHT,
    fontSize: dimensions.width / 12.6 // 30
  },
  TINY: {
    color: colors.HIGHLIGHT,
    fontSize: dimensions.width / 17.18 // 22
  }
}